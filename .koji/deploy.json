{
  "deploy": {
    "subdomain": "uuid-qr.withkoji.com",
    "frontend": {
      "output": "build",
      "commands": [
        "cd frontend",
        "yarn install",
        "export NODE_ENV=production && yarn build"
      ]
    }
  }
}